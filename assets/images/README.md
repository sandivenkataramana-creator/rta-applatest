Place the two minister portrait images in this folder with the exact filenames below so the app can load them as bundled assets:

- revanth.png  (Sri A. Revanth Reddy)
- ponnam.png   (Sri Ponnam Prabhakar)

Recommended image size and format:
- PNG or JPG
- Square aspect ratio (e.g., 300x300) for best circle crop results

After adding the files run:

```
flutter pub get
flutter run -d windows
```

If you'd like, I can add the actual image files now if you upload them or allow me to embed them into the repo.